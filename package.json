{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^10.12.18",
    "babel-loader": "^8.0.5",
    "lerna": "^3.10.7",
    "typescript": "^3.3.1"
  },
  "version": "1.0.0",
  "main": "index.js",
  "repository": "",
  "author": "Robin Schuster <r.schuster@cognigy.com>",
  "license": "MIT",
  "workspaces": [
    "packages/socket-client",
    "packages/webchat-client",
    "packages/webchat"
  ],
  "scripts": {
    "build:socket-client": "cd packages/socket-client && npm run build",
    "build:webchat-client": "cd packages/webchat-client && npm run build",
    "build:webchat": "cd packages/webchat && npm run bundle && npm run bundle:datepicker-plugin",
    "build": "npm run build:socket-client && npm run build:webchat-client && npm run build:webchat",
    "clean": "rm -rf ./packages/*/lib && rm -f ./packages/webchat/dist/*.js"
  },
  "dependencies": {
    "moment": "^2.24.0"
  }
}
